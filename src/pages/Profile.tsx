export function Profile({ isVisible: _isVisible }: { isVisible: boolean }) {
  return (
    <div className="flex flex-col items-center px-6 py-8">
      <h2 className="font-display text-3xl font-semibold text-midnight-navy">Profile</h2>
      <p className="mt-2 text-sm text-midnight-navy/60">Settings and preferences — coming in Phase 9.</p>
    </div>
  )
}
