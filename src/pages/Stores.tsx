export function Stores({ isVisible: _isVisible }: { isVisible: boolean }) {
  return (
    <div className="flex flex-col items-center px-6 py-8">
      <h2 className="font-display text-3xl font-semibold text-midnight-navy">Stores</h2>
      <p className="mt-2 text-sm text-midnight-navy/60">Vancouver store map — coming in Phase 11.</p>
    </div>
  )
}
